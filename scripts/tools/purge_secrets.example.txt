# Each block is original text, newline, replacement, newline.
# Keep the real values only in a local, untracked copy such as
# `scripts/tools/purge_secrets.local.txt`.

# Example database URL
postgresql://actual-secret-value
postgresql://<redacted-database-url>

# Example provider key
actual-provider-secret
<redacted-provider-secret>

# Example JWT token
eyJactualTokenValue
<redacted-jwt-token>
