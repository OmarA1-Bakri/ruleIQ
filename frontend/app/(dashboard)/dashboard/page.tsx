'use client';

/**
 * Dashboard page — stub.
 * The real dashboard route does not exist yet at this path.
 * This stub allows tests to import and mock the module without resolution errors.
 */

import React from 'react';

export default function DashboardPage() {
  return (
    <div data-testid="dashboard-page-stub">
      <h1>Dashboard</h1>
      <p>Dashboard page (stub — not yet implemented at this route)</p>
    </div>
  );
}
